*pro.user


**/.vs/
.vscode/
build/
config.tests/*/.qmake.stash
config.tests/*/Makefile
.qtcreator/
scripts/build/
instructions.png
instructions.txt

# Mockups: runnable QML plus the screenshots taken from it, used to review a screen
# before touching the real one (see docs/notes, the 5.7.0 entry). Kept on disk because
# the next redesign starts from them; kept out of the repo because they belong to one
# release, and 1.7 MB of PNGs and captured stdout is nothing a clone needs.
mockups/

# A jom build log that was captured by hand. Regenerated by any build, useful to keep
# locally while chasing a compile error, useless to everyone else.
# NOTE: this file is already TRACKED, so the line below does nothing on its own --
# it also needs: git rm --cached scripts/build_output.txt
scripts/build_output.txt
